# workflow
# Should have Name
name: Hello

on:
  workflow_dispatch: 
# jobs
# Should be inside the workflow

jobs:
  say-hello:
    runs-on: ubuntu-latest
  steps:
  - name: say hello to everyone
    run: echo "hello dosto"
# steps
# Should be inside the inside the jobs

# Actions
# Should be inside the Steps
